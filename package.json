{
   "scripts": {
  "test": "cypress run",
  "posttest": "npx mochawesome-merge cypress/reports/html/*.json > mochawesome.json && npx marge mochawesome.json --reportDir=cypress/reports/html --reportFilename=index.html"
},
  "devDependencies": {
    "cypress": "^14.5.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
